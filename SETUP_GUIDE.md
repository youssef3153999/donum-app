# دليل إعداد تطبيق ارض ماب — رياكت نيتيف خام

هذا المجلّد يحوي هيكل تطبيق رياكت نيتيف خام لنظام أندرويد، يستخدم نفس قاعدة بيانات الموقع تماماً. أيّ أرض تُضاف من الموقع تظهر في التطبيق فوراً والعكس.

---

## ما هو موجود في هذا المجلد

ملفّات تي إس و ريأكت جاهزة:

- ملف بيكدج جي سون مع كلّ المكتبات اللازمة.
- ملف آب تي إس إكس فيه شريط تبويب سفلي (خريطة، أراضيّ، حسابي).
- شاشة الخريطة مع طبقة فيليبس على الأقمار الصناعية من ماببوكس.
- شاشة الأراضي الخاصّة بالمستخدم.
- شاشة الحساب مع زرّ تسجيل الخروج وتبديل اللغة.
- شاشة تسجيل الدخول وإنشاء الحساب.
- وحدات سوبابيس و ترجمة و حسابات هندسية مشتركة.

ما هو **ناقص** يجب توليده بالأمر الذي سيأتي:

- مجلّد أندرويد كامل (غرادل، الإكس إم إل، الملفّات الأصلية).
- ملف آي أو إس (لن نستخدمه).
- ملفات إعدادات أكثر تفصيلاً.

---

## الخطوة الأولى — تجهيز الحاسوب

أنت تحتاج لتثبيت هذه الأشياء قبل أي شيء آخر:

أوّلاً نود جي إس بإصدار 18 أو أعلى. عادي إذا عندك نسخة أحدث.

ثانياً جافا جي دي كي 17 بالضبط. ليس 11 ولا 21. حمّله من رابط أدوبتيوم.

ثالثاً أندرويد ستوديو. هذا تطبيق رسمي من جوجل. حمّله من موقع ديفلوبر دوت أندرويد دوت كوم. بعد التثبيت افتحه ومن خلال إس دي كي مانجر ثبّت كذا حزمة:
- أندرويد إس دي كي بلاتفورم 34
- أندرويد إس دي كي بيلد تولز 34
- أن دي كي اللازم لماببوكس.

رابعاً متغيّرات البيئة. أضف هذه إلى ملف النظام:
- أندرويد هوم يشير إلى مكان تثبيت إس دي كي.
- جافا هوم يشير إلى مكان تثبيت جي دي كي 17.
- باث يضمّ مجلّد إس دي كي بلاتفورم تولز.

---

## الخطوة الثانية — توليد ملفّات أندرويد الأصلية

افتح طرفية في مجلّد ارد ماب آر إن واكتب:

```
npx @react-native-community/cli@latest init ardmap_tmp --version 0.75.4 --skip-install
```

هذا الأمر يولّد مشروع رياكت نيتيف كامل في مجلّد ارد ماب تي إم بي.

افتح المجلّد المولَّد وانسخ منه فقط:
- مجلّد أندرويد بأكمله إلى مجلّدنا الحالي ارد ماب آر إن.
- ملف غيت إيجنور إذا أردت.

لا تنسخ بيكدج جي سون لأن عندنا واحد جاهز بالاعتمادات الصحيحة.

ثم احذف مجلّد ارد ماب تي إم بي.

---

## الخطوة الثالثة — تركيب المكتبات

من مجلّد ارد ماب آر إن:

```
npm install
```

سيستغرق بضع دقائق. حجم نود مودولز يقارب 600 ميغابايت.

---

## الخطوة الرابعة — إعداد ماببوكس

ماببوكس يحتاج رمزين اثنين منفصلين:

الأوّل رمز عام يبدأ بحرفين بي كي. هذا الرمز موجود عندك أصلاً من الموقع. ضعه في ملف إس آر سي / ليب / إنف دوت تي إس مكان كلمة بي كي بنقاط.

الثاني رمز سرّي للتحميل يبدأ بحرفين إس كي. هذا رمز جديد يجب توليده من حساب ماببوكس. اذهب إلى:
account.mapbox.com → toggle on "Downloads:Read" → نسخ الرمز.

ثم افتح ملف غرادل بروبرتيز داخل مجلّد أندرويد وأضف هذا السطر:

MAPBOX_DOWNLOADS_TOKEN=sk.eyJ ...

ثم افتح ملف غرادل الأعلى مستوى داخل مجلّد أندرويد وضمن قسم ربوسيتوريز أضف:

```
maven {
  url 'https://api.mapbox.com/downloads/v2/releases/maven'
  authentication { basic(BasicAuthentication) }
  credentials {
    username = 'mapbox'
    password = project.properties['MAPBOX_DOWNLOADS_TOKEN']
  }
}
```

---

## الخطوة الخامسة — إعداد سوبابيس

ضع رابط سوبابيس ومفتاح الأنون في ملف إس آر سي / ليب / إنف دوت تي إس. خذهما من حساب سوبابيس عندك (إعدادات → إيه بي آي).

---

## الخطوة السادسة — التشغيل أوّل مرّة

وصّل هاتف أندرويد بكابل يو إس بي وفعّل وضع المطوّر وتصحيح الأخطاء عبر يو إس بي. تحقّق من ظهوره بكتابة:

```
adb devices
```

ثم في طرفية أولى شغّل خادم مترو:

```
npm start
```

وفي طرفية ثانية شغّل التطبيق على الهاتف:

```
npm run android
```

أوّل بناء يأخذ من 5 إلى 15 دقيقة. الجولات التالية أسرع بكثير.

---

## الخطوة السابعة — بناء ملف إيه بي كاي للنشر

أوّلاً ولّد مفتاح توقيع:

```
keytool -genkeypair -v -storetype PKCS12 -keystore ardmap-release.keystore -alias ardmap -keyalg RSA -keysize 2048 -validity 10000
```

احفظ كلمة المرور في مكان آمن لا يضيع. لو ضاعت لن تستطيع تحديث التطبيق في المتجر أبداً.

انقل ملف المفتاح إلى android/app/ وأضف في غرادل بروبرتيز:

```
MYAPP_RELEASE_STORE_FILE=ardmap-release.keystore
MYAPP_RELEASE_KEY_ALIAS=ardmap
MYAPP_RELEASE_STORE_PASSWORD=*****
MYAPP_RELEASE_KEY_PASSWORD=*****
```

ثم ابنِ:

```
npm run build:bundle
```

سيظهر ملف إيه إيه بي في:
android/app/build/outputs/bundle/release/app-release.aab

هذا الملف يُرفع على متجر جوجل بلاي.

---

## ما تبقّى من العمل البرمجي

شاشات لم تُكتب بعد ويمكن إضافتها لاحقاً:
- شاشة إنشاء أرض جديدة برسم الحدود.
- شاشة تفاصيل الأرض الكاملة مع معرض الصور.
- شاشة المراجعة للأدمن.
- إشعارات دفع لمشتركي ماببوكس.
- وضع داكن وفاتح قابل للتبديل.
- البحث والفلاتر داخل الخريطة.

كلّ هذه ممكنة. هذا فقط الهيكل الأوّلي ليبدأ التطبيق بالعمل.

---

## مشاكل شائعة

إذا فشل بناء غرادل بسبب ماببوكس تحقّق من رمز إس كي.

إذا ظهرت شاشة بيضاء عند تشغيل التطبيق افتح أدب لوغكات وابحث عن الخطأ. غالباً يكون متغيّر بيئة سوبابيس فارغ.

إذا لم تظهر الخريطة تحقّق من أن رمز بي كي موجود في إنف دوت تي إس.

إذا فشل تركيب المكتبات بسبب صدامات نسخ احذف مجلّد نود مودولز وملف بيكدج لوك دوت جي سون ثم أعد تشغيل أمر التركيب.
